from setuptools import setup, find_packages

setup(
    name='avalon_project',
    version='1.0',
    description='It\'s avalon bro',
    author='Yo momma',
    author_email='jagdeep@mit.edu',
    packages=find_packages(),
    install_requires=[
    ],
    extras_require={
    },
)